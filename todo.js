
const [counter, setCounter] = useState(0)

setCounter(++counter)
sorre:{
    user:{}
    loading:false

    reducer:{
        setUwer:(state, aciton)=>{
                
        }
        isLoading:(state)=>{
                state.loading=true
        }
    }
}

export const user = ({})=>{
        <div>{prop.name}</div>
}

setUwer(user)